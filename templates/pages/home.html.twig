{% extends 'base.html.twig' %}

{% block body %}
    <div class="jumbotron text-center">
        <h1>mon agence immo</h1>
        <p>Elle est super cette agence</p>
    </div>
    <div class="container">
        <h2>Les derniers biens</h2>
        <div class="flex row">
            {% for property in  latestProperties %}
                <div class="col-3">
                    {% include 'property/_property.html.twig' %}
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
