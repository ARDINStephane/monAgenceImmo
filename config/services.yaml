imports:
    - { resource: "packages/services/app.yaml" } # must be loaded first
    - { resource: "packages/services/" }

parameters:
    locale: 'fr'
    base_urls: 'http://127.0.0.1:8000'
    google_recaptcha_site_key: '%env(GOOGLE_RECAPTCHA_SITE_KEY)%'


